# AGENTS.md

Canonical instructions for this repo live in `CLAUDE.md` — read that file,
not this one. Codex-specific RTK command reference: `~/.codex/RTK.md`.

This file exists only so Codex-based tools have a conventional entry point;
it intentionally does not duplicate `CLAUDE.md`'s content, to avoid the two
drifting out of sync.
